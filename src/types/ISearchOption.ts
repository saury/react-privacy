export interface ISearchOption {
  mode?: string;
  productid: number | string;
  culturecode: string;
  token: string;
  studentid: number | string;
  coursetype?: string;
  hadacceped?: string;
  accepted?: string;
  failed?: string;
  servicever?: string;
  rejected?: string;
}
